# Live Arena Server

Node.js WebSocket server for the live arena, chat, and virtual bets.

## Run
1. Install dependencies: `npm install`
2. Start: `npm start`

The server serves the static site from `../web` and exposes the WebSocket endpoint at `/ws`.
